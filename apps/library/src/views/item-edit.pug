form.library_item(
  hx-put='/api/update-item',
  hx-target='this',
  hx-swap='outerHTML',
  hx-trigger='submit',
  hx-vals=`{ "id": "${item.id}" }`
)
  input(type='text', name='title', hx-swap='none', value=item.title)
  div
    button save
    button cancel
